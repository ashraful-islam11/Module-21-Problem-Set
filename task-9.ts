// todo:   problem no - 9. 
// Problem Statement:
// একটি number positive না negative তা ternary operator দিয়ে বের করো।
// Input:
// num = 5
// Output:
// Positive

let num : number = 5; 

// if( num > 0) ? 'Positive' : '';
let result = num > 0 ? 'Positive' : '';
console.log(result);


